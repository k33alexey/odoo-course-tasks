<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="tutor_reassign_wizard_view_form" model="ir.ui.view">
        <field name="name">tutor.reassign.wizard.form</field>
        <field name="model">tutor.reassign.wizard</field>
        <field name="arch" type="xml">
            <form string="Reassign Lessons">
                <group>
                    <field name="from_tutor_id"
                           options="{'no_create': True}"
                           domain="[('id', '!=', to_tutor_id), ('lesson_ids.state', '=', 'planned')]"/>
                    <field name="to_tutor_id"
                           options="{'no_create': True}"
                           domain="[('id', '!=', from_tutor_id)]"/>
                </group>

                <group string="Lessons to Reassign">
                    <field name="line_ids" nolabel="1">
                        <list editable="bottom" create="false" delete="false">
                            <field name="reassign" nolabel="1"/>
                            <field name="lesson_id" column_invisible="True"/>
                            <field name="name"/>
                            <field name="appointment_date"/>
                            <field name="student_id"/>
                            <field name="state" widget="badge"
                                   decoration-muted="state == 'planned'"
                                   decoration-warning="state == 'in_progress'"
                                   decoration-success="state == 'done'"
                                   decoration-danger="state == 'cancel'"/>
                        </list>
                    </field>
                </group>
                <footer>
                    <button name="action_reassign" string="Reassign" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="tutor_reassign_wizard_action" model="ir.actions.act_window">
        <field name="name">Reassign Tutor</field>
        <field name="res_model">tutor.reassign.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
